server {
    listen 80;
    listen [::]:80;
    server_name _;

    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;

        # WebSocket-Upgrade (zwingend für das Live-Spiel)
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        proxy_read_timeout 3600s;   # WS-Verbindungen offen halten
        proxy_send_timeout 3600s;
    }
}

# Für HTTPS: Zertifikat via certbot/ACME ergänzen und listen 443 ssl; hinzufügen.
# (Simon: passt zu deiner ACME-Automatisierung – Domain + PSE analog wie im SAP-Kontext.)
